# dsh-univer-workspace-plugin bundle patch. The plugin row is mounted by the
# univer-workspace-harness composition; configuration (workspace origin is
# resolved through the harness workspaceAuth service) arrives with the
# capability implementation.

- insert:
    - id: univer-workspace
      name: 'dsh-univer-workspace-plugin'
