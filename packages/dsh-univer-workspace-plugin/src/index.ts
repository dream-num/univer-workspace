import type { Context } from "@deepseek-ai/cordis";

export const name = "dsh-univer-workspace-plugin";

export function apply(_context: Context): void {
  // Space ↔ dsh-workspace reconciliation, the univer toolset operating
  // remote Workspace Units, bundled skills, and the browser-facing API
  // proxy routes are mounted here in the capability phases.
}
