import type { Context } from "@deepseek-ai/cordis";

export const name = "dsh-univer-workspace-skin-plugin";

export function apply(_context: Context): void {
  // The skin is browser-only; the host half stays empty by design.
}
