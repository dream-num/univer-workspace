# Provision the Prometheus data source at Grafana startup.
apiVersion: 1
datasources:
  - name: Prometheus
    uid: Prometheus
    type: prometheus
    access: proxy # Grafana forwards queries to Prometheus.
    url: http://prometheus:9090 # Compose service name within the container network.
    isDefault: true
