## 1. Seven DSH-native Skill sources

- [ ] 1.1 Add `base`, `board`, `doc`, `sheet` and `slide` under `apps/dsh-univer-work/skills/<name>/SKILL.md`; rewrite their executable paths to the exact accepted Worktree/Unit/review, execute/inspect, API, Office, Typst, SVG, resource and screenshot/lint operations, and verify the five required/forbidden operation rows plus Base/Board/Doc/Sheet/Slide semantic anchors against both source files and their future packed copies.
- [ ] 1.2 Add `embed` and `cross-unit-formula` under the same root; preserve explicit ResourceRef or qualifier/Source identity, lazy read-only Source loading and calculation/read-back ordering, and verify both Topic rows forbid staging and unrelated exchange/generation/inspection operations while requiring exact execute/API/screenshot/review references; assert all seven exact names, no second `core`, no CLI syntax and no unsupported frontmatter.

## 2. Native registration lifecycle

- [ ] 2.1 Add the fixed two-field package loader and one fiber-owned registration effect that validates all seven definitions before side effects, registers each with bundled source/runtime provider/default invocation and cleans up exact disposers in reverse order; use the real SkillRegistry to verify malformed-definition atomic failure, normalized metadata, same-name native precedence and complete disposal.

## 3. Executable-surface drift gate

- [ ] 3.1 Build the real complete ToolRegistry from the ten explicit accepted operation owners, extract literal `workspace_*` references from every source and packed body, and enforce the per-Skill required/forbidden matrix plus semantic-anchor assertions; test that a missing requirement, forbidden/unknown/renamed operation, stale chart owner, CLI command/option, `skills get` or checkout-absolute path fails without credential or Workspace access.

## 4. Package closure

- [ ] 4.1 Include the seven Markdown sources exactly once in the prebuilt tarball and extend package verification to reject missing/duplicate files, sibling CLI Skill reads, install-time generation, supplemental/dynamic provider resources and absolute build paths; inspect the packed artifact from an unrelated cwd.

## 5. Keyless installed behavior

- [ ] 5.1 Extend real Native and Code Mode ToolRuntime tests plus the isolated tarball smoke to use empty isolated project/DSH/AGENTS Skill roots, list and load every Skill through the real `skill` consumer without a Workspace grant or network, permit default/scoped provider lookups while proving registration is independent of their results and adds no provider/root/watcher, then dispose the Host and prove all unshadowed contributions disappear.

## 6. Responsibility and repository gates

- [ ] 6.1 Update the DSH application responsibility/package documentation for the seven-Skill boundary and run focused app checks, OpenSpec strict validation, repository SDK-baseline/typecheck/test/build, Workspace CLI package smoke and `git diff --check`, preserving the CLI's existing eight Skill files and behavior.
