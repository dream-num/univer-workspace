## 1. Confirm predecessor contracts

- [x] 1.1 Verify Changes 1–6 and 9 are implemented at their real public exports and package gates, record the authenticated owner, local provider/Session-cwd policy helper, Slide content execution, worker entry, render-page/browser resolver and error/lifecycle adapter names, and stop at the owning Change instead of creating a duplicate when any prerequisite is absent.

## 2. Extend Client Core SVG controls

- [x] 2.1 Add optional signal, canonical local-root, source/aggregate-asset limits and apply value limits to the existing `workspace-client-core/svg` exports; implement one private canonicalize/open/same-regular-file-identity/`remaining + 1` bounded reader that counts actual bytes, and test symlink/replacement/size races, boundaries, compiler/measurement cancellation, awaited close, shared Slide execution forwarding, result-unknown/no-replay and unchanged unsignalled CLI behavior.

## 3. Add closed SVG tools and budgets

- [x] 3.1 Register exactly `workspace_svg_compile` and `workspace_svg_apply` with closed snake_case argument/output schemas, exact-own-key/cross-field validation, fixed argument/source/asset/code/result budgets and lossless Native/Code Mode rendering; verify raw/page, replace/add, both tools' inline/file generated unions, exact serialized apply-value allowance and canonical outcomes through real Cordis `ToolRuntime` tests.

## 4. Enforce local paths and approvals

- [x] 4.1 Reuse the Change 5 positive local-provider and calling Session-cwd gate for source/recursive assets, implement compile-output policy revalidation plus one compile approval or one combined apply file/Draft approval, save apply's exact in-memory program before executing it once, and test read-only, remote provider, missing cwd, traversal/symlink escape, approval denial/change, save failure with zero remote work, approved replacement and zero pre-approval source/browser/credential/Workspace effects.

## 5. Preserve safe failures and lifecycle

- [x] 5.1 Extend the shared exact error adapter and single Host owner with the frozen SVG allowlist and closed file-confirmed apply-partial projection; plant SVG/code/path/credential/license/browser sentinels and use real ToolRuntime race gates to distinguish body-observed post-file cancellation (`workspace-svg-apply-partial`) from registry-only abort after confirmed body return (`ABORTED` plus fixed non-fabricating guidance), while also verifying ordinary/value-limit failure, commit unknown, owner-only drain, no replay/delete, `ABORTED_BEFORE_DISPATCH`, credential-generation retirement and bounded dispose transcripts.

## 6. Close the installed runtime graph

- [x] 6.1 Extend the application build/package graph to inline reachable private Core and exact SVG facade code while reusing the packaged worker, render page, resolved browser dependencies and license resolver; verify the tarball contains no bare private import, CLI source/artifact, sourcemap, absolute checkout path, browser binary or font bundle, that the SVG reachable graph introduces no Office/Typst native asset while existing Office/Typst closures remain independently verified, and that every emitted/runtime reference resolves.

## 7. Verify source and CLI compatibility

- [x] 7.1 Run focused DSH SVG tests, Client Core SVG/content tests, CLI `compile-svg` command/built-entrypoint/package tests, application typecheck/build and package verification; confirm existing CLI source/assets, `--out` ordering, real/estimated measurement, warnings/lints, apply outcomes and arbitrary-cwd behavior remain unchanged.

## 8. Exercise the installed tools and update documentation

- [x] 8.1 Install the prebuilt tarball into an isolated local DSH profile and, from an unrelated Session cwd through real `ToolRuntime`, compile nested assets in estimate and explicitly resolved real-browser modes, save and apply the same exact program once to a keyless fake Workspace/Collaboration endpoint, exercise file-partial/cancellation/unknown/dispose, inspect model-visible snapshots, then update affected application/Core README and architecture responsibility text and run `openspec validate add-dsh-svg-generation-tools --strict`, relevant repository gates and `git diff --check`.
