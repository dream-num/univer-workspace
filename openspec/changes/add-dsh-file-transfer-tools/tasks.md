> Prerequisite: complete and verify `add-dsh-univer-work-plugin-shell`, `add-dsh-univer-work-authentication`, and `add-dsh-space-node-tools` before implementation.

## 1. 贯穿 Client Core 文件传输 cancellation

- [x] 1.1 为 `WorkspaceBlobFeature` get/upload/download、`WorkspaceAssetFeature.download`、Blob stable-identity/recovery、source stream 与 download target 追加向后兼容的 optional `AbortSignal`：传给每个 HTTP/content request，在每个 recovery/stream/commit 边界检查；取消与已 dispatch reserve/PUT/status/complete/read-back unknown 相撞时，立即以 `idempotencyKey`、完整 public upload intent 及已知 `uploadId`/state 重新封装 `workspace-result-unknown`，绝不透传丢失身份的 transport-only unknown 或启动下一 status/read-back/retry/complete，`discard()` 始终清理 handle/temp。在 `file-transfer.test.ts` 与 `files.test.ts` 固定每个 cancellation edge 的 safe detail、request count、source/signed content/response write/pre-commit/late commit/cleanup，运行 Client Core `typecheck`/`test` 并确认所有 no-signal cases 不变。

## 2. 固定 closed tools、download policy 与 local Session path gate

- [x] 2.1 在 `apps/dsh-univer-work` 添加四个 operation-specific definitions 和 closed Node/Blob/Operation/upload/download fragments，复用前序 `defineClosedWorkspaceTool()` 使 schema root `additionalProperties: false` 且 body exact-own-key reject；对 confining `ctx.fs.sandboxMode` 要求 public sandbox-policy service。download `tools/pre-execute` 先读调用 Session current policy，`read-only` 在 provider/argument/path/ask/body/credential/I/O 前抛既有 safe typed `workspace-file-policy-denied`；随后以 exact rc.2 public `ctx.fs instanceof LocalFileSystem` 正向验证 bare local/in-process sandbox subclass，non-local 以 fixed `workspace-local-filesystem-required` 在 zero ask/resolve/contains/processPath/credential/Core/Host I/O 下拒绝，identity 通过后才可把 undefined mode 当 bare local。再对 `workspace-write` 做 current policy root + Session cwd、对 danger/bare 做 Session cwd 的 pure args + `ctx.fs.resolve/contains` preflight 后 ask，不 stat/open/read/create destination、不调用 `processPath`，也不缓存 policy/path。approved body 完成 exact immutable-argument validation 后按 current policy→constructor identity→canonical path 顺序重做三道 application gate，仅通过后调用 `processPath`/Core；覆盖 policy 变窄/变宽、provider change、symlink 漂移，不实现 cross-approval controller、二进制 FS 或 escalation。真实 ToolRuntime/filesystem/policy tests 另覆盖四个 schemas、direct extra keys、confining-without-policy composition failure、read-only denial error info、undefined-mode E2B-like no-ask/no-resolve rejection、bare/subclass acceptance、workspace-write dual-root、danger cwd、relative/absolute/symlink、missing/non-file source、agent-less 与 invalid output-before-render；运行 `pnpm --filter dsh-univer-work test`。

## 3. 交付 Blob metadata 与 upload tools

- [x] 3.1 复用 Change 2 authenticated resolver 和 `WorkspaceBlobFeature` 实现 `workspace_blob_get` 与 `workspace_blob_upload` canonical values/value-only rendering，不复制 endpoint/parser/recovery；ToolRuntime + temporary local source/fake Server cases覆盖严格 Blob identity、default/explicit idempotency、parent/name/media type、reserve→PUT→status→complete→Resource read-back、same-intent bounded recovery、terminal/mismatch/source-size failures，并断言 Core 已确认的写不重放、settled failure 不由 ToolRuntime retry。

## 4. 交付 Blob/Asset downloads 与 approval

- [x] 4.1 实现 `workspace_blob_download`、`workspace_asset_download`，并让同一 fiber-owned `tools/pre-execute` listener 对 upload 直接返回一次 fixed safe `ask`、对两种 download 仅在 Task 2 policy→local-constructor→path preflight 通过后返回一次 `ask`、对 Blob get/其他 tools 调用 `next()`；用真实 ToolRuntime、local cwd 与 fake Server/CDN 覆盖 allowed-once、read-only no-provider/no-ask、undefined-mode non-local no-path/no-ask、malformed/outside no-ask、reject/cancel/unavailable/no-channel、无 double approval、Blob availability/metadata、Asset sign envelope/URL/cross-origin cookie isolation、exact bytes/`0600`、existing/racing output no-clobber、explicit force atomic replace、stream/size failure temp cleanup，断言所有 denial 不触发 credential/Core/Host I/O且 body 在 `processPath` 前再次验证 constructor。

## 5. 收敛 transfer errors、signals 与 lifecycle

- [x] 5.1 扩展同一 application execution wrapper/Host owner：exact allowlist common/Blob/Asset/Server/path codes 与 fixed `workspace-file-policy-denied`、safe detail fields，其余映射 `workspace-file-operation-failed`；pre-execute policy/path promise 仅观察 DSH caller signal且不留状态，accepted body 融合并保留 caller/owner signals，跟踪 body policy/path→credential→Core→cleanup。Cordis tests 固定 pre-execute HarnessError code、caller abort、read-only denial 无 path/credential/I/O detail，body pre-write cancelled/disposing、cancelled download cleanup、dispatched upload unknown 保留 public intent/known Upload Session identity且 request count 不增长、caller-aborted late upload/commit success=`ABORTED` + inspect-before-retry finalizer、owner-only confirmed success、dispose unregister/body abort/drain；在 policy/provider/error/signed URL/cause/detail 植入 password/cookie/`Set-Cookie`/grant/bytes sentinel，断言 results/events/approval 无反射且无 cross-approval state、detached request/stream/handle/temp/listener/timer/Job/cache。

## 6. 固定 installed artifact 与真实 local-file smoke

- [x] 6.1 更新 manifest、build 与 `package:verify`，使 exact `@deepseek-ai/dsh-fs`、`dsh-fs-local`、`dsh-sandbox`、`dsh-sandbox-policy@0.1.1-rc.2` 和 DSH/Cordis external、reachable private Core Blob/Asset/files inlined，禁止 workspace bare Core、CLI/Server source、remote adapter、worker/native/render/Office/Web/Skill/later resources；扩展隔离 tarball `package:smoke`，用真实 installed ToolRuntime、bare LocalFS/in-process sandbox/E2B-like undefined-mode provider、policy、temporary Agent Session cwd 和 keyless fake Workspace 响应执行四个 tools，固定 read-only typed error info且 no-provider/no-ask/no-path，non-local fixed error且 zero ask/resolve/contains/processPath/credential/Core/Host I/O，bare/subclass constructor acceptance、workspace-write dual-root、approval 后 policy 收窄及 body constructor recheck、danger cwd、closed/extra args、single approval、upload/download/no-clobber/force、cancelled dispatched upload public-intent unknown/no-follow-up、secret/cleanup/dispose transcript，并运行 DSH app `build`/`package:verify`/`package:smoke`。

## 7. 更新职责文档并运行兼容性 gate

- [x] 7.1 更新 `apps/dsh-univer-work/README.md` 与 `packages/client-core/README.md`，记录四个 names、Session-cwd/Host-local 范围、download sandbox-policy mode matrix、single approval/无 escalation、overwrite、signal/result-unknown 与明确非职责；依次运行 Client Core `typecheck`/`test`/`build`、DSH app `typecheck`/`test`/`build`/`package:verify`/`package:smoke`、`pnpm --filter univer-workspace-cli test -- application-command-contracts.test.ts`、`pnpm package:workspace-cli`、`pnpm typecheck`、`pnpm test`、`pnpm build` 和 `git diff --check`，确认 CLI Blob/Asset command/output/Session/local-path/package、Server/OpenAPI、SDK baseline、release 与 deployment 不变。
