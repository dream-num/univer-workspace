> Prerequisite: implement and verify all twelve accepted `add-dsh-univer-work-plugin-shell`, authentication, Space/Node, Worktree/Unit, file-transfer, content-runtime, Office, Typst, SVG, render-verification, API/resource-discovery and bundled-unit-topic-Skills Changes. If an owned capability is absent or failing, stop and return the failure to that Change; do not implement it here.

## 1. Freeze the executable outcome manifest

- [x] 1.1 Add one typed, data-only parity manifest that inventories every production `apps/cli` root/subcommand, positional argument, option and result form at Workspace `a01adf28...`; classify each as a mapped product argument/result, a named DSH-native shell mechanism, or presentation-only evidence. Map product rows to exactly one of the twelve owner Changes, exact 42 tools/eight Skills and acceptance case ids; explicitly map config/Session/daemon/version/help/Skill path, `screenshot setup`, resource cache, password login, viewer override and JSON/text/path presentation to credential/profile/lifecycle/catalog/deployment/no-retention/canonical-value evidence without new tools. Add a focused validator that fails on omitted/unclassified CLI surface, unknown/duplicate owner, absent operation/Skill, missing case id, or test-owned replacement implementation.

## 2. Lock the complete DSH catalog and schemas

- [x] 2.1 Activate the complete source application with empty project/user Skill roots through real DSH registries, normalize only nondeterministic metadata, and check in one canonical registry snapshot containing exactly 42 tool names/descriptions/full parameter and output schemas plus eight Skill summaries; join registered definitions with the checked manifest classification and probe every consequential/non-consequential tool through real `tools/pre-execute` invalid/ask/deny/allow or delegation paths. Test missing/extra/renamed/duplicate/shadowed contributions, open schema drift, invalid Skill references and post-dispose removal, then run `pnpm --filter dsh-univer-work test`.

## 3. Exercise every outcome through production boundaries

- [x] 3.1 Reuse one minimal deterministic fixture corpus to run every keyless manifest route and one representative success per authenticated outcome through real Native and Code Mode dispatch with no model key or external public network: fake only isolated loopback Workspace/Collaboration remote authority/transport plus approval and credential endpoints; use the real temporary local DSH filesystem service, production parsers/public boundaries, packaged worker/runtime child, real Office native binding, real Typst/SVG compilers, installed browser/render page, datasets/assets and Skills. Assert lossless canonical values, authoritative identities/read-backs and no `apps/cli/src/*`, private Core source/dist, adjacent checkout or operation-replacement fixture import.

## 4. Index cross-cutting parity cases

- [x] 4.1 Require each applicable manifest row to select final-boundary approval, allowlisted/unlisted failure, caller/owner cancellation, dispatched result-unknown/no-replay, confirmed partial file, secret-sentinel and non-local execution-world cases from its owner contract; run the indexed Native/Code Mode suite and fail with owner Change/case diagnostics on missing coverage or semantic mismatch, while scanning only the owner-defined plugin-controlled transcript surfaces and proving all accepted bodies drain.

## 5. Gate the packed resource and size closure

- [x] 5.1 Extend `package:verify` to traverse final manifest/Host/worker entries, bare imports and package-relative links plus explicit native/browser/dataset/Skill resources; enforce declared exact externals, the allowlisted file/import classes, no source/checkout/absolute fallback, and fixed `16,777,216` packed bytes, `67,108,864` unpacked bytes and `256` entries from `npm pack --dry-run --json --ignore-scripts`. Report measured totals/largest offenders and fail rather than auto-updating a budget, allowlist or snapshot.

## 6. Run the final installed parity smoke

- [x] 6.1 Build and pack once, install that exact prebuilt tarball with scripts disabled into a temporary local DSH profile and unrelated cwd, verify bundle membership/Loader row and run the complete catalog plus keyless/authenticated suites without source resolution; execute three in-process activate/use/dispose cycles and one fresh-process start/normal-stop deadline, asserting every cycle exposes 42 tools/eight Skills then leaves no registration, body, worker, browser, handle, listener, timer, in-flight credential mutation, temporary output or unhandled rejection. Assert a valid persistent owner GrantRecord survives dispose/restart and only logout removes it; the harness, not plugin disposal, resets fixture credentials. Run `pnpm --filter dsh-univer-work package:verify` and `package:smoke`.

## 7. Publish only verified responsibility facts

- [x] 7.1 Update `apps/dsh-univer-work/README.md` from or against the parity manifest with the frozen baseline, outcome table, exact counts, passing package limits, local-only boundary and non-goals; run focused app typecheck/test/build/package gates, all affected Client Core checks, `pnpm package:workspace-cli`, Workspace CLI smoke/parity tests, `pnpm typecheck`, `pnpm test`, `pnpm build`, OpenSpec strict validation and `git diff --check`, confirming no CLI command/artifact/release, Server/Browser/OpenAPI/database/deployment, SDK baseline or plugin publication workflow change.
