## 1. Confirm prerequisites and frozen source contracts

- [x] 1.1 Verify Changes 1–4 are implemented and their shell/authentication/tool owner, Worktree/Unit identities and private Core exports match this design; recheck DSH `0.1.1-rc.2` ToolRuntime policy/log/cancellation order, credential-record update event, published content-inspection query/results, runtime-pool signal absence/worker-child path and SDK `1.0.0-beta.2`, then run pre-edit `pnpm --filter @univerjs/univer-workspace-client-core typecheck` and `pnpm --filter dsh-univer-work typecheck`, stopping instead of creating a parallel owner or changing the baseline if a prerequisite differs.

## 2. Add Core source and execution cancellation

- [x] 2.1 Append optional signals only to Worktree/Trunk target resolution and content-execution feature methods reached by inspection/execute, plus optional `maxValueBytes`/`maxValueDepth` forwarding for execute; pass signals through authenticated HTTP/runtime read-write inputs without changing unrelated Core operations or CLI daemon RPC, and add focused Core/CLI tests for pre-abort, mid-target abort, no later request, prepared-program/budget forwarding and unchanged unsignalled execute/inspect payloads and results; verify with the focused `runtime-source`/`content-execution` Core tests and `apps/cli/test/content-execution.test.ts` plus CLI inspection cases.

## 3. Add Core runtime, image, and commit cancellation

- [x] 3.1 Extend runtime operation/resolver inputs, per-key queue, acquire/synchronize/read/write steps and embedded-image uploader/externalizer with optional signal checks while always awaiting frozen-pool operations; validate an optional execute value as lossless JSON within caller bytes/depth before any upload/replacement/commit; preserve ordinary BASE64 fallback, propagate in-flight upload uncertainty, and record confirmed uploads so cancellation before the next upload/replacement/commit emits structured `workspace-content-partial-side-effect`, invalidates the lease and performs no compensating delete/re-upload/replay; keep commit unknown dominant after confirmed uploads, then test a multi-image fixture for first-confirmed abort before next and before commit, in-flight unknown, commit unknown, caller/owner settlement, every other signal boundary, quiescence, confirmed race, unsignalled attempts, awaited close and secret-free failures; run the complete Client Core suite.

## 4. Compose the current Host runtime generation

- [x] 4.1 Add one lazy current runtime generation to the existing DSH Host effect, using a package-relative worker entry, a direct current-grant credential closure with origin check, and non-empty `UNIVER_LICENSE` override or synchronized application-owned default; retire/close the generation on the exact credential-record event and on disposal without exposing grant/license to tools, add rotation synchronization without printing the credential, and verify reuse, grant replace/delete during active work, no stale cookie, one close per generation, accepted-body drain and sentinel secrecy with real Cordis tests.

## 5. Add structured inspection

- [x] 5.1 Declare exact root/query/selector schemas and the honest non-recursive DSH result projection for `workspace_content_inspect`; implement the frozen pure-validation order with a 524,288-byte total argument gate, at most 64 selectors/ranges, beta.2 exact cell-A1 syntax, safe-area overflow and 100,000-cell limit, plus the 8 MiB output bound; add the complete seven-union/nested/JSON/Unit/recursive-children validator through depth 64, resolve authoritative targets with the fused signal, and test huge single/combined args, malformed A1→inspection invalid, grammar-valid safe-area overflow→limit, all discriminants, zero-based/scope cases, malformed/valid/depth/wrong-Unit output, no truncation, Native/Code Mode, cancellation and safe errors.

## 6. Add approved Draft execution and shared failure/lifecycle behavior

- [x] 6.1 Add `workspace_content_execute` with the three-field closed schema and ordered shared pure policy/body validator for 524,288-byte total arguments then 262,144-byte code, one fixed secret-safe `ask`, Draft execution with Core's 8,388,000-byte pre-side-effect value budget, final 8 MiB/64-depth envelope, enumerated errors including partial side effect, exact safe identity and total finalizers; test huge args before approval, approval/content/value outcomes, no replay/disposal, and real ToolRuntime branches proving confirmed body late success→`ABORTED`, caller/owner partial throw→partial code plus orphan guidance, and in-flight upload/commit unknown throw→`workspace-result-unknown`, alongside split non-reflection sentinels.

## 7. Deliver the worker-backed tarball closure

- [x] 7.1 Add the Host/worker multi-entry package build using the CLI build's CJS-global treatment, inline reachable private Core/exact SDK JavaScript, copy the resolved runtime-pool `worker-child.mjs`, resolve the exact `@univerjs-pro/engine-formula-rust-binding` from the installed `@univerjs-pro/engine-formula-rust` owner manifest, externalize and declare/copy that exact binding, and define `__UNIVER_RUST_FORMULA_LOCAL_BINDING_FALLBACKS__ = false`; reject bare Core/workspace imports, absolute checkout/source paths, CLI daemon/Session and future Office/render/generation resources, then install the tarball in an isolated profile and from an unrelated temporary cwd with no workspace `node_modules` run keyless real ToolRuntime inspection plus no-mutation/confirmed/unknown execution through `worker.js`, `worker-child.mjs`, binding and license/credential resolvers, verifying credential replacement and bounded disposal with package verify/smoke.

## 8. Preserve documentation and repository compatibility

- [x] 8.1 Update the DSH and Client Core READMEs plus root runtime-license wording to describe the now-delivered inspection/execute scope, worker/license ownership, durable code-argument warning, cancellation ceiling and exclusions without claiming later Changes; run `pnpm --filter @univerjs/univer-workspace-client-core typecheck`, Core tests/build, `pnpm --filter dsh-univer-work typecheck`, test/build/package verify/smoke, relevant CLI execute/inspect/daemon tests, `pnpm package:workspace-cli`, CLI package verify/smoke, `pnpm typecheck`, `pnpm test`, `pnpm build`, `openspec validate add-dsh-content-runtime-tools --strict` and `git diff --check`, confirming Server/Browser/OpenAPI/database/deployment/CLI command/release behavior and the exact SDK baseline remain unchanged.
