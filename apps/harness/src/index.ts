import type { Context } from "@deepseek-ai/cordis";

export const name = "univer-workspace-harness";

export function apply(_context: Context): void {
  // The OAuth login flow against the Workspace authorization endpoints, the
  // per-User workspace session credential store, the workspaceAuth service,
  // and the identity/session routes are mounted here with the harness core.
}
